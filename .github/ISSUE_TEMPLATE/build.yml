name: "🏗️ Build & CI/CD"
description: "Changes that affect the build system or CI/CD pipelines."
labels: ["build", "ci/cd"]
body:
  - type: dropdown
    id: category
    attributes:
      label: "Category"
      options:
        - Dependencies
        - GitHub Actions
        - Docker/Containerization
        - Build Scripts
  - type: textarea
    id: description
    attributes:
      label: "Change Description"
      placeholder: "Describe the updates to dependencies or workflow files."
